#include "Table.hpp"
#include <iostream>

#include "Boule.hpp"

using namespace std;

Table::Table() : m_x(0), m_y(0), m_x2(2.54), m_y2(1.27)
{
}

Table::Table(double x, double y, double x2, double y2) : m_x(x), m_y(y), m_x2(x2), m_y2(y2)
{
}

void Table::innitialisation()
{
    int n = 0;
    while(n>)
}
